# TASK_020 — Apple-редизайн экрана «Счета»

- **Номер:** TASK_020
- **Название:** Accounts Screen Apple Redesign
- **Статус:** DONE
- **Создан:** 2026-07-28
- **Реализация начата:** 2026-07-28
- **Реализация завершена:** 2026-07-28 (595 passed, 0 failed по всем 8
  файлам тестов; проверено в браузере)

---

## 1. Контекст

`TASK_019` (Apple-редизайн экрана «Аналитика») завершена, закоммичена
(`d7b7c48`/`1f6e446`), запушена и проверена на production (`finance-v162`).
Следующий запрошенный пользователем этап — полный редизайн экрана «Счета»
(`#scrAccounts`) в едином Apple-стиле Главной/Аналитики/Категорий, с
сохранением всей существующей бизнес-логики и модели данных.

## 2. Проверка репозитория (до изменений)

- Ветка: `main`, синхронизирована с `origin/main` (`1f6e446`).
- `git status`: незакоммиченные `.claude/launch.json` (личный конфиг
  превью) и удалённый `icon.svg`, untracked `.DS_Store` — не относятся к
  задаче, не тронуты (по прямому указанию пользователя).
- `sw.js`: `CACHE = 'finance-v162'`, соответствует `HEAD`.
- Baseline тестов: `node tests/*.test.js` — **595 passed, 0 failed**
  (8 файлов).

## 3. Найденные существующие компоненты и архитектура (переиспользуются)

- Экран — `#scrAccounts` (`index.html`), рендерится
  `renderAccountsScreen()` → `renderDistChart()` + `renderAccounts()` +
  `renderDistList()`.
- Карточка «Общий капитал» — `.capital.cap-simple` (`#accTotalCapital`/
  `#accCapCur`/`#capEye2`), населяется в `renderAccountsScreen()`.
  Глобальный класс `.capital` разделяется с экраном «Статистика»
  (`#statsHero`) — трогать глобально нельзя.
- Карточка «Личный профиль A-Lex» (открывает профиль по клику,
  `TASK_017`) — `.drawer-head` в боковом меню, точный градиент
  `linear-gradient(135deg,#a79cf7 0%,#8fb0f6 55%,#8fdde3 100%)`. Это и
  есть искомая существующая карточка с фиолетово-голубым градиентом —
  переиспользуется буквально (не новый похожий цвет).
- Группы счетов (Наличные/Карты/Крипто + пользовательские) —
  `state.accountGroups` (`seedAccountGroups()`/`accGroups()`), рендер —
  `renderAccounts()`: до задачи группы были плоскими `.grp-head`+
  `.grp-body` без карточки-обёртки, счета внутри — отдельные карточки
  `.acc3` (бордер+свой цветной градиент), без вложенных inset-разделителей.
- Drag & drop — `setupAccountSort()`/`liftAccCard()`/
  `commitAccountOrder()`, за ручку `.acc-handle`, активен только в режиме
  `accEditMode` (переключается кнопкой, была `.acc-edit-toggle` с текстом
  «↕ Изменить порядок»).
- Архив — блок `📦 Архив` внутри `renderAccounts()`, отдельная ветка без
  карточки-обёртки; архивирование/восстановление — `archiveAccount()`/
  `archiveFromEditor()` в редакторе счёта (`#accOverlay`) — вне границ
  задачи, не тронуты.
- Круговая диаграмма + список — `#distChart` (Chart.js doughnut,
  `distData()`/`AF.Services.Account.distribution()`) + `#distList`
  (`renderDistList()`, `.dist-row`) внутри `.panel` «Распределение
  капитала».
- Стиль для переиспользования: `#scrRecords{background:var(--home-bg)}`
  (`TASK_009`) и `#scrCharts{background:var(--home-bg)}` +
  `#scrCharts .panel{border-radius:22px;box-shadow:var(--fincard-shadow)}`
  (`TASK_019`) — тот же приём применён к `#scrAccounts`. Существующий
  переиспользуемый компонент секции-заголовка с действием справа —
  `.sec-head` (`<span>`/`<a>`), уже используется на Главной («По
  категориям») — расширен поддержкой `<button>` для «Счета Изменить».
  Существующий прогресс-бар `.progress`/`.fill` (используется у целей/
  бюджетов) — переиспользован для закраски строк распределения капитала
  по проценту.

## 4. Цель

Полный визуальный редизайн экрана «Счета» под единый Apple-стиль
(Apple Wallet / Настройки iOS), без изменения структуры данных,
бизнес-логики расчётов, валют, импорта/экспорта и поведения приложения.

## 5. Границы

**Включено:**

1. Карточка «Общий капитал» (`.capital.cap-simple` внутри `#scrAccounts`)
   — только оформление: точный градиент `.drawer-head` через
   scoped-override `#scrAccounts .capital{background:linear-gradient(
   135deg,#a79cf7 0%,#8fb0f6 55%,#8fdde3 100%);...}` (глобальный
   `.capital`, используемый `#statsHero`, не тронут). Глаз скрытия
   баланса (`#capEye2`), сумма (`#accTotalCapital`), валюта
   (`#accCapCur`) и вся логика (`updateEye()`, `state.hideAmounts`) —
   без изменений.
2. Фон экрана — `#scrAccounts{background:var(--home-bg)}` +
   `.scroll-area:has(>#scrAccounts.active){background:var(--home-bg)}`
   (тот же приём, что `#scrRecords`/`#scrCharts`, `TASK_009`/`TASK_019`).
   Панель «Распределение капитала» получает `.fincard`-тень/радиус через
   `#scrAccounts .panel{...}` (глобальный `.panel`, используемый другими
   экранами — Бюджеты/Аналитика/Health Score — не тронут).
3. Каждая группа счетов — отдельная карточка `.acc-cat` (белый фон,
   радиус 20px, `var(--fincard-shadow)`): слева иконка категории (цветной
   кружок + SVG, подобрана по названию для встроенных групп «Наличные»/
   «Карты»/«Крипто», иначе — из палитры по индексу; чисто визуальное
   вычисление, не хранится в `state.accountGroups` — модель данных не
   меняется) + название + число счетов; справа — сумма группы, шеврон
   разворота, кнопка-меню «⋯» (переименование/удаление группы — прежняя
   `openGroupEdit()`, не изменена).
4. Раскрытие группы: счета показываются внутри той же карточки как
   строки списка с inset-разделителями (начинаются после иконки, как в
   iOS), а не отдельными card-в-card, как раньше. Плавная анимация
   раскрытия/сворачивания — новая `toggleAccGroup()` на CSS-transition
   `max-height` (значение измеряется через `scrollHeight`, класс
   `.acc-cat-body-wrap`), без полного перерендера `#accList` на каждый
   клик (в отличие от старого поведения, которое просто исключало
   `.grp-body` из разметки при `isCol`).
5. Кнопка «↕ Изменить порядок» заменена на компактную «Изменить» справа
   от заголовка «Счета» (переиспользован `.sec-head`, тот же паттерн, что
   «По категориям» на Главной). Логика режима сортировки (`accEditMode`,
   `setupAccountSort()`, `liftAccCard()`, `commitAccountOrder()`) —
   без изменений в алгоритме, только переименованы CSS-хуки (`.acc3` →
   `.acc-cat-row` в трёх местах, `.grp-body` **сохранён без изменений**
   как функциональный класс для строк реальных/безгруппных счетов —
   архивные строки его не получают, как и раньше, чтобы не участвовать в
   `commitAccountOrder()`).
6. Карточка «Распределение капитала»: увеличены внутренние отступы
   (`padding:22px 20px` вместо `18px`), заголовок чуть крупнее
   (scoped `#scrAccounts .panel h3`). Список распределения (`#distList`)
   — современные inset-разделители (`rgba(0,0,0,.06)`/тёмная тема
   `rgba(255,255,255,.08)`, начинаются после точки-маркера), суммы и
   проценты выровнены по правому краю фиксированной шириной, и под
   каждой строкой — тонкий прогресс-бар (переиспользован существующий
   `.progress`/`.fill`), закрашенный цветом счёта на ширину его доли —
   пункт «закрашиваемость цветом согласно проценту».
7. Bump версии кэша `sw.js`: `finance-v162` → `finance-v163`.

**Не включено:**

- `AF.Services.Account.*` (`balance`, `share`, `distribution`,
  `active`) — не изменены.
- Модель данных `state.accounts`/`state.accountGroups` — без изменений
  полей и формата.
- Редактор счёта (`#accOverlay`, `openAcc()`/`saveAcc()`/`delAcc()`/
  `archiveFromEditor()`) и просмотр счёта (`#detailOverlay`,
  `openAccountGraph()`/`renderDetail()`) — не редизайнились, вне границ
  задачи (задание касалось списка на главном экране «Счета»).
- Круговая диаграмма (`renderDistChart()`, Chart.js `doughnut`,
  `cutout:'66%'`) — сохранена как есть, не заменена.
- Логика создания счёта/группы, архивирования, `totalCapital()` — без
  изменений.
- Тёмная тема — не приоритет задачи, но не сломана (проверено).

## 6. Критерии готовности

- Карточка «Общий капитал» использует точный градиент карточки «Личный
  профиль A-Lex», сохраняет глаз/сумму/валюту/логику.
- Фон экрана и зона под навигацией — единый светло-серый фон Главной.
- Каждая группа счетов — отдельная белая карточка с иконкой, названием,
  числом счетов, суммой, шевроном и меню.
- Раскрытие группы — плавная анимация, счета внутри карточки с inset-
  разделителями.
- Кнопка «Изменить порядок» заменена на компактную «Изменить» рядом с
  заголовком «Счета».
- Круговая диаграмма сохранена, карточка вокруг неё — современнее
  (отступы/типографика), список распределения — аккуратные разделители,
  выровненные суммы/проценты, закраска цветом по проценту.
- Все существующие тесты (595) проходят без регрессий.
- Проверено в браузере: светлая/тёмная тема, мобильная ширина (375px),
  раскрытие/сворачивание категорий, режим сортировки и реальный
  drag-переход счёта между позициями, меню группы (переименование),
  архивирование/восстановление счёта, создание/удаление счёта, консоль
  без ошибок.

## 7. План проверок

1. `node tests/*.test.js` (все файлы) — без регрессий.
2. `node --check` для извлечённого инлайн-скрипта `index.html` —
   синтаксис.
3. Браузерная проверка на локальном сервере: экран «Счета» с демо-
   данными, раскрытие/сворачивание каждой категории, режим «Изменить» +
   реальный drag счёта (подтверждено программной симуляцией
   `commitAccountOrder()` через фактический DOM-порядок), меню группы
   (открытие редактора «Наличные»), создание нового счёта, архивирование
   и восстановление счёта, удаление тестового счёта, светлая/тёмная
   тема, мобильная ширина 375px, консоль без ошибок.
4. По готовности — коммит, публикация (push), production-проверка
   `sw.js` (ожидаемая версия кэша), запись результата в этот файл.

## 8. Реализация

### 8.1. Изменённые файлы

- `index.html`:
  - CSS: полностью переписан блок «Accounts v2» — новые классы
    `.acc-cat`/`.acc-cat-head`/`.acc-cat-ic`/`.acc-cat-info`/
    `.acc-cat-sum`/`.acc-cat-chev`/`.acc-cat-menu`/`.acc-cat-body-wrap`/
    `.acc-cat-row`/`.acc-row-ic`/`.acc-row-body`/`.acc-row-left`/
    `.acc-row-right`/`.acc-row-amt`/`.acc-row-bal`/`.acc-row-pct`/
    `.acc-row-dots` заменили `.acc3*`/`.grp-head`/`.grp-arrow`/`.grp-nm`/
    `.grp-sum`/`.grp-edit`/`.acc-toolbar`/`.acc-edit-toggle`. Класс
    `.grp-body` **сохранён** (функциональный хук для drag/commit).
    Добавлены scoped-правила `#scrAccounts{background:var(--home-bg)}`,
    `.scroll-area:has(>#scrAccounts.active){background:var(--home-bg)}`,
    `#scrAccounts .panel{border-radius:22px;box-shadow:var(--fincard-shadow);
    padding:22px 20px}`, `#scrAccounts .capital{background:...точный
    градиент .drawer-head...}` (тот же приём, что `TASK_009`/`TASK_019`).
    `.sec-head` расширен правилом для `button`. `.dist-row` переписан на
    двухстрочный формат (строка данных + `.dist-progress`).
  - Разметка `#scrAccounts`: `.acc-toolbar` с `.acc-edit-toggle` заменены
    на `.sec-head` с `<button id="accEditBtn">Изменить</button>`.
  - JS: `accountCardHtml()` → `accountRowHtml()` (строка списка вместо
    карточки); новая `accGroupIcon(name,idx)` (иконка по названию/
    палитре); `renderAccounts()` переписан — каждая группа оборачивается
    в `.acc-cat`, тело всегда рендерится (не исключается при
    `isCol`, в отличие от старого кода) и анимируется через новую
    `toggleAccGroup(gid)` (CSS `max-height`-transition,
    `wrap.scrollHeight` + `requestAnimationFrame`); архивный блок также
    обёрнут в `.acc-cat` с тем же механизмом, но без класса `.grp-body`
    на теле (как и раньше исключён из `commitAccountOrder()`).
    `liftAccCard()`/`setupAccountSort()`/`commitAccountOrder()` —
    точечно заменены только селекторы `.acc3` → `.acc-cat-row` (3 места);
    `.grp-body` в `commitAccountOrder()`/`liftAccCard()` — без изменений.
    `renderDistList()` — добавлен `.progress`/`.fill`-бар на ширину
    `pct%`, цвет счёта.
  - При написании `toggleAccGroup()` **проактивно исключён** класс
    бага `TASK_018` (`$$(...).find(...)` — `$$` возвращает `NodeList`,
    без `.find()`): использован `$('#accList .acc-cat[data-grp="..."]')`
    вместо `$$(...).find(...)`.
- `sw.js` — `CACHE`: `finance-v162` → `finance-v163`.

### 8.2. Границы — соблюдены

- `AF.Services.Account.*` — не изменены (проверено: только UI-обёртки
  `accBalance()`/`curCode()`/`plural()` переиспользованы как есть).
- `state.accounts`/`state.accountGroups` — формат полей не изменён;
  `accGroupIcon()` вычисляется на лету при рендере, не персистится.
- Редактор счёта/группы, детальный граф счёта, архивирование — не
  тронуты (только переименованы CSS-классы строк, которые они кликают
  косвенно через делегирование `renderAccounts()`).
- Глобальные `.capital`/`.panel` — не изменены, только scoped-override
  `#scrAccounts .capital`/`#scrAccounts .panel`.

## 9. Результаты проверок

- `node tests/*.test.js` (все 8 файлов) — **595 passed, 0 failed**
  (без изменений количества — задача не добавляла тестовых файлов,
  только визуальный редизайн; сервисы/бизнес-логика не затронуты).
- `node --check` — синтаксис извлечённого инлайн-скрипта `index.html`
  — без ошибок.
- Браузерная проверка (`http://localhost:8767`, `finance-repo` из
  `.claude/launch.json`, файл не изменялся):
  - Демо-данные: карточка «Общий капитал» — точный градиент
    (фиолетово-голубой, идентичен карточке профиля в drawer), сумма/
    валюта/глаз на месте и рабочие.
  - Три встроенные группы отрендерены как отдельные карточки: «Наличные»
    (зелёная иконка-банкнота), «Карты» (синяя иконка-карта, тот же
    силуэт, что у иконки вкладки «Счета» нижней навигации), «Крипто»
    (оранжевая иконка-монета) — с числом счетов и суммой.
  - Раскрытие/сворачивание карточки категории — плавная анимация,
    шеврон разворачивается на 180°, проверено на «Наличные» (раскрыта →
    свёрнута → снова раскрыта), консоль без ошибок на каждом шаге.
  - Режим «Изменить»: кнопка переключается в «Готово», строки счетов
    показывают ручку-перетаскивание вместо «⋯».
  - Реальный порядок счетов проверен программно: добавлены два тестовых
    счёта в группу «Наличные», DOM-порядок строк изменён (аналогично
    результату реального drag), вызван `commitAccountOrder()` —
    `state.accounts` и `groupId` обновились корректно, тестовые счета
    удалены после проверки.
  - Меню группы («⋯» → «Наличные») открывает существующий редактор
    группы (`openGroupEdit()`) с полями «Название группы»/«Сохранить»/
    «Удалить группу» — без изменений.
  - Создание нового счёта («+ Счёт», банк, баланс 500€) — счёт появился
    в карточке группы, круговая диаграмма и список распределения
    обновились (два сегмента, 79%/21%, с прогресс-барами под каждой
    строкой, суммы и проценты выровнены).
  - Архивирование созданного счёта — появилась карточка «Архив» (серая
    иконка-коробка, без кнопки меню, как и должно быть для недоступной
    для переименования системной группы), счёт показан приглушённым,
    подпись «В архиве».
  - Восстановление из архива и последующее полное удаление счёта —
    состояние вернулось к исходному демо-набору без остатков.
  - Тёмная тема (`toggleTheme()`) — карточки категорий, строки, тени,
    inset-разделители, прогресс-бары распределения и капитал-карточка
    корректно адаптированы (проверено скриншотом всего экрана).
  - Мобильная ширина 375×812 — без горизонтального переполнения.
  - `read_console_messages` — без ошибок на протяжении всей проверки
    (создание/удаление счетов, архивирование, drag-тест, смена темы).
- Production-верификация: см. §10 ниже (заполняется после push).

## 10. Production-верификация

_Заполняется после push и подтверждения раздачи `finance-v163` на
`https://adar4026.github.io/finance/`._
