# TASK_022 — Apple-редизайн экрана «Бюджеты»

- **Номер:** TASK_022
- **Название:** Budgets Screen Apple Redesign
- **Статус:** DONE
- **Создан:** 2026-07-28
- **Реализация начата:** 2026-07-28
- **Реализация завершена:** 2026-07-28 (709 passed, 0 failed по всем 10
  файлам тестов; проверено в браузере)

---

## 1. Контекст

`TASK_021` (Apple-редизайн профиля) завершена, закоммичена (`6099da2`),
запушена и проверена на production (`finance-v164`, коммит `73d1fd4`).
Следующий запрошенный пользователем этап — полный редизайн экрана
«Бюджеты» (`#scrBudgets`) в едином Apple-стиле Главной/Аналитики/
Категорий/Счетов, с сохранением всей существующей бизнес-логики и
модели данных. Пользователь отдельно подчеркнул: «Счета» — визуальный
и архитектурный эталон, стиль для Бюджетов не придумывается заново, а
переиспользуется буквально.

## 2. Проверка репозитория (до изменений)

- Ветка: `main`, синхронизирована с `origin/main` (`73d1fd4`).
- `git status`: незакоммиченные `.claude/launch.json` (личный конфиг
  превью, изменён — порт `finance-repo` на 8767) и удалённый `icon.svg`,
  untracked `.DS_Store` — не относятся к задаче, не тронуты (по прямому
  указанию пользователя, как и в предыдущих задачах).
- `sw.js`: `CACHE = 'finance-v164'`, соответствует `HEAD`.
- Baseline тестов: `node tests/*.test.js` (все 9 файлов) — **675 passed,
  0 failed**.

## 3. Найденные существующие компоненты и архитектура (переиспользуются)

- Экран — `#scrBudgets` (`index.html:1613`), рендерится `renderBudgets()`
  (`index.html:3229`): герой `#budgetTotal` + список категорий `#budCards`
  + кнопка `#addBudgetBtn` (уже класс `.add-dashed`, тот же, что «+ Счёт»/
  «+ Группа» на экране «Счета» — переиспользуется буквально, без
  изменений).
- Бизнес-логика (не трогается): `budgetMonthRange()` (месяц по
  глобальному `anchor`, общему с «Аналитикой»/остальными периодами),
  `budgetSpent(catId)`, `budgetIds()`, `budgetTotals()`,
  `budgetLevel(pct)` (`ok`/`warning`≥80%/`critical`≥100%),
  `budgetColor(level,fallback)`. Отдельно существует неподключённый к UI
  чистый сервис `AF.Services.Budget` (`js/services/budget_service.js`,
  `spent()`/`status()`/`recommend()`) с похожей, но не идентичной
  логикой (использует `t.amount` вместо `txBase(t)`) — как и
  `AF.Services.Analytics.compare()` в `TASK_019`, это дублирующая чистая
  функция вне UI-пути; не трогается, вне границ задачи.
- Редактор бюджета — `#budgetEditOverlay` (`index.html:2394`,
  `openBudgetEdit()`/`saveBudget()`/`deleteBudget()`/
  `renderBudCatStrip()`) — универсальный `.sheet`-паттерн, общий для
  всех редакторов приложения (счёт, категория, цель); как и в
  `TASK_020` (редактор счёта не редизайнился), редактор бюджета остаётся
  вне границ этой задачи.
- Карточка «Осталось в бюджете» — `.bud-hero` внутри `#budgetTotal`,
  сейчас красится через `var(--cap-grad)` (общий фиолетовый токен,
  разделяемый с `.capital` по умолчанию, `.ana-hero`, `.cashflow-card`,
  `goals-summary`, `.cal-forecast` — **не** тот же градиент, что у
  «Общего капитала» на «Счетах», хотя визуально похож). Искомый эталонный
  градиент — точный `#scrAccounts .capital{background:linear-gradient(
  135deg,#a79cf7 0%,#8fb0f6 55%,#8fdde3 100%);box-shadow:0 12px 28px
  rgba(110,120,220,.28),inset 0 1px 0 rgba(255,255,255,.5)}`
  (`index.html:1362`, тот же самый, что и у карточки «Личный профиль
  A-Lex» в шторке, `.drawer-head`, `TASK_017`), подключается тем же
  scoped-override приёмом, что и в `TASK_020`.
- Карточки категорий — `.bud-card` (`index.html:1197`): сейчас
  `border:1px solid var(--line)` + отдельное правило `.bud-card.over{
  border:2px solid rgba(239,68,68,.42)}` — толстая красная рамка вокруг
  всей карточки при перерасходе, именно то, что пользователь просил
  убрать. Внутренние элементы (`.bc-head`/`.bc-ic`/`.bc-info`/`.bc-nm`/
  `.bc-sub`/`.bc-right`/`.bc-rl`/`.bc-rv`/`.bc-edit`/`.bc-bar`/`.bc-fill`)
  уже несут корректную семантику статуса (`.bc-rl.over`/`.bc-rv.over` —
  красный текст, `.bc-fill` — динамический цвет по `budgetColor()`) — не
  меняются.
- Фон экрана: приём `#scrRecords{background:var(--home-bg)}` +
  `.scroll-area:has(>#scr***.active){background:var(--home-bg)}`
  (`TASK_009`/`TASK_019`/`TASK_020`) — применяется аналогично к
  `#scrBudgets`.
- Общий переключатель периода (`#periods`) и строка навигации месяца
  (`#navrow`, `index.html:1535`) — общие для всех экранов (в `#subhead`,
  вне `.screen`), уже скрывают `#periods` для Бюджетов
  (`showScreen()`, `index.html:4161`), `#navrow` остаётся видимым
  (месяц/стрелки) — не дублируется и не трогается, как и в
  `TASK_019`/`TASK_020` (это общий компонент, не специфичный для одного
  экрана).
- Пустое состояние — `.bud-empty` (`index.html:3250`, эмодзи 🎯 + текст),
  тот же паттерн, что `.goals-empty`/`.notif-empty` — сейчас без
  карточки-обёртки, будет обёрнуто в белую карточку для соответствия
  «белые карточки, минимум визуального шума».

## 4. Цель

Полный визуальный редизайн экрана «Бюджеты» под единый Apple-стиль,
эталон — экран «Счета» (`TASK_020`), без изменения структуры данных,
бизнес-логики расчётов (месяц, лимит, потрачено, остаток, перерасход,
категории) и поведения создания/редактирования/удаления бюджета.

## 5. Границы

**Включено:**

1. Фон экрана — `#scrBudgets{background:var(--home-bg)}` +
   `.scroll-area:has(>#scrBudgets.active){background:var(--home-bg)}`
   (тот же приём, что `#scrRecords`/`#scrCharts`/`#scrAccounts`).
2. Карточка «Осталось в бюджете» (`.bud-hero` внутри `#budgetTotal`)
   получает класс `.capital` в JS (`bt.className='capital bud-hero'`) и
   scoped-override `#scrBudgets .capital{background:linear-gradient(
   135deg,#a79cf7 0%,#8fb0f6 55%,#8fdde3 100%);box-shadow:0 12px 28px
   rgba(110,120,220,.28),inset 0 1px 0 rgba(255,255,255,.5);padding:22px
   20px}` — точно тот же градиент/тень, что у «Общего капитала» на
   «Счетах» (не новый похожий цвет). Собственные правила `.bud-hero`
   сокращаются до структурных (`.bh-top`/`.bh-lbl`/`.bh-badge`/
   `.bh-val`/`.bh-bar`/`.bh-fill`/`.bh-foot`/`.bh-empty`) — фон/радиус/
   тень/цвет текста/margin теперь приходят от общего `.capital`.
   Реальные данные (остаток/перерасход/потрачено/лимит/% использования/
   прогресс-бар) — без изменений в разметке и логике `renderBudgets()`.
3. Карточки категорий (`.bud-card`) переоформлены в стиль «белая
   карточка + `var(--fincard-shadow)`» (как `.acc-cat`/`.fincard`), без
   рамки `border:1px solid var(--line)` и **без** толстой красной рамки
   при перерасходе (`.bud-card.over` с `border:2px solid` — удаляется).
   Статус перерасхода/приближения к лимиту остаётся понятным через
   существующие `.bc-rl.over`/`.bc-rv.over` (красный текст) и
   `.bc-fill` (цвет по `budgetColor()`) — не через рамку карточки.
4. Пустое состояние (`.bud-empty`) получает белый фон/тень/радиус
   (`var(--fincard-shadow)`), тот же язык, что остальные карточки экрана
   — без выдуманных данных, тот же текст и эмодзи.
5. Кнопка «Добавить бюджет» (`#addBudgetBtn`, класс `.add-dashed`) — уже
   идентична кнопкам «+ Счёт»/«+ Группа» на «Счетах», изменений не
   требуется (проверяется тестом, что класс не расходится).
6. Bump версии кэша `sw.js`: `finance-v164` → `finance-v165`.

**Не включено:**

- `budgetMonthRange()`/`budgetSpent()`/`budgetIds()`/`budgetTotals()`/
  `budgetLevel()`/`budgetColor()` — расчётная логика не меняется.
- `state.budgets` — формат данных не меняется.
- Редактор бюджета (`#budgetEditOverlay`, `openBudgetEdit()`/
  `saveBudget()`/`deleteBudget()`/`renderBudCatStrip()`) — не
  редизайнится, вне границ задачи (как и редактор счёта в `TASK_020`).
- Общий переключатель периода/строка навигации месяца (`#periods`/
  `#navrow`) — общие для всех экранов, не трогаются.
- `AF.Services.Budget` (`js/services/budget_service.js`) — не
  подключённый к UI дублирующий чистый сервис, не трогается (аналогично
  `AF.Services.Analytics.compare()` в `TASK_019`).
- Тёмная тема — не приоритет задачи, но не должна сломаться (проверяется).

## 6. Критерии готовности

- Карточка «Осталось в бюджете» использует точно тот же градиент/тень,
  что карточка «Общий капитал» на «Счетах» (через переиспользование
  класса `.capital`, а не новый подобранный цвет).
- Фон экрана и зона под навигацией — единый светло-серый фон Главной.
- Карточки категорий — белые, с тенью `.fincard`, без рамки, без
  толстой красной рамки при перерасходе; статус читается по цвету
  текста/прогресс-бара.
- Пустое состояние — аккуратная белая карточка, без выдуманных данных.
- Кнопка «Добавить бюджет» визуально идентична кнопкам добавления на
  «Счетах» (общий класс).
- Вся финансовая логика не изменилась: месяц навигация, лимит,
  потрачено, остаток, перерасход, создание/редактирование/удаление
  бюджета — работают как раньше.
- Все существующие тесты (675) проходят без регрессий + новые тесты
  задачи.
- Проверено в браузере: светлая/тёмная тема, мобильная ширина (375px),
  переключение месяца, обычный бюджет, почти исчерпанный лимит,
  перерасход, редактирование, добавление, консоль без ошибок.

## 7. План проверок

1. `node tests/*.test.js` (все файлы) — без регрессий.
2. `node --check` для извлечённого инлайн-скрипта `index.html` —
   синтаксис.
3. Браузерная проверка на локальном сервере (`finance-repo`, порт 8767):
   экран «Бюджеты» с демо-данными — герой-карточка (градиент,
   остаток/перерасход/%/прогресс-бар), список категорий (обычный,
   почти-исчерпанный ≥80%, перерасход), переключение месяца стрелками
   `#navrow`, создание нового бюджета, редактирование лимита, удаление
   бюджета, пустое состояние (после удаления всех бюджетов), светлая/
   тёмная тема, мобильная ширина 375px, консоль без ошибок.
4. По готовности — коммит, публикация (push), production-проверка
   `sw.js` (ожидаемая версия кэша), запись результата в этот файл.

## 8. Реализация

### 8.1. Изменённые файлы

- `index.html`:
  - CSS-блок «Budgets» (`index.html:1185`) переписан: `.bud-hero` больше
    не задаёт свой фон/радиус/тень/цвет — все они приходят от общего
    `.capital`; собственные правила `.bud-hero` сокращены до структурных
    (`.bh-top`/`.bh-lbl`/`.bh-badge`/`.bh-val`/`.bh-bar`/`.bh-fill`/
    `.bh-foot`/`.bh-empty`). `.bud-card` — убраны `border:1px solid
    var(--line)` и правило `.bud-card.over{border:2px solid
    rgba(239,68,68,.42)}` (толстая красная рамка при перерасходе),
    добавлены `border-radius:20px;box-shadow:var(--fincard-shadow)` (стиль
    `.acc-cat`/`.fincard`). `.bud-empty` получил `background:var(--card);
    border-radius:20px;box-shadow:var(--fincard-shadow)` — белая карточка
    вместо голого текста на фоне экрана. Внутренние правила статуса
    (`.bc-rl.over`/`.bc-rl.warn`/`.bc-rv.over`/`.bc-bar`/`.bc-fill`) — без
    изменений (уже несли верную семантику).
  - Добавлены scoped-правила рядом с аналогичными для `#scrAccounts`
    (`index.html:1358`): `#scrBudgets{background:var(--home-bg)}`,
    `.scroll-area:has(>#scrBudgets.active){background:var(--home-bg)}`,
    `#scrBudgets .capital{background:linear-gradient(135deg,#a79cf7 0%,
    #8fb0f6 55%,#8fdde3 100%);box-shadow:0 12px 28px
    rgba(110,120,220,.28),inset 0 1px 0 rgba(255,255,255,.5);padding:22px
    20px}` — буквально та же градиентная строка и та же тень, что у
    `#scrAccounts .capital` (проверено тестом посимвольно, не «похожий
    цвет»). Глобальные `.capital` (используется `#statsHero`) и `.panel`
    (другие экраны) не тронуты.
  - Комментарий экрана в разметке обновлён на «Screen: Budgets (TASK_022
    Apple redesign)» (`index.html:1612`), по образцу `TASK_019`/`TASK_020`.
  - JS: единственное изменение в `renderBudgets()` —
    `bt.className='bud-hero'` → `bt.className='capital bud-hero'`
    (переиспользование общего компонента вместо собственного фона).
    Разметка карточки категории: `class="bud-card ${over?'over':''}"` →
    `class="bud-card"` (класс `over` для рамки больше не нужен — статус
    и так подставляется через `.bc-rl`/`.bc-rv`/`.bc-fill`, переменная
    `over` в теле функции сохранена и используется как прежде).
    `budgetMonthRange()`/`budgetSpent()`/`budgetIds()`/`budgetTotals()`/
    `budgetLevel()`/`budgetColor()`/`openBudgetEdit()`/`saveBudget()`/
    `deleteBudget()`/`renderBudCatStrip()` — не изменены ни на символ.
- `sw.js` — `CACHE`: `finance-v164` → `finance-v165`.
- `tests/budgets_screen.test.js` — новый файл, 34 статические
  regex-проверки: посимвольное совпадение градиента/тени `#scrBudgets
  .capital` с `#scrAccounts .capital`, отсутствие `.bud-hero{background:
  var(--cap-grad)...}` как отдельной сиреневой карточки, отсутствие
  `.bud-card.over{...}` и класса `over` в разметке карточки, наличие
  `var(--fincard-shadow)` на `.bud-card`/`.bud-empty`, сохранность всех
  расчётных функций бюджетов (по сигнатуре), единственность правила
  `.add-dashed{...}` (не задублировано под «Бюджеты»), версия кэша `sw.js`.

### 8.2. Границы — соблюдены

- Расчётная логика (`budgetMonthRange`/`budgetSpent`/`budgetIds`/
  `budgetTotals`/`budgetLevel`/`budgetColor`) и `state.budgets` — не
  изменены.
- Редактор бюджета (`#budgetEditOverlay`) и общий переключатель
  периода/строка навигации месяца (`#periods`/`#navrow`) — не тронуты,
  вне границ задачи.
- `AF.Services.Budget` (`js/services/budget_service.js`) — неподключённый
  к UI дублирующий чистый сервис, не тронут (аналогично прецеденту
  `TASK_019` с `AF.Services.Analytics.compare()`).
- Глобальные `.capital`/`.panel`/`.add-dashed` — не изменены, только
  добавлен новый scoped-override `#scrBudgets .capital`.

## 9. Результаты проверок

- `node tests/*.test.js` (все 10 файлов) — **709 passed, 0 failed**
  (675 существующих без регрессий + 34 новых в
  `tests/budgets_screen.test.js`).
- `node --check` — синтаксис извлечённого инлайн-скрипта `index.html`
  — без ошибок.
- Браузерная проверка (`http://localhost:8767`, `finance-repo` из
  `.claude/launch.json`, порт уже настроен пользователем, файл не
  изменялся):
  - Демо-данные (`loadDemo()` через консоль с замоканным `confirm()`,
    т.к. нативные диалоги подавлены безголовым браузером): герой-карточка
    «Осталось в бюджете» — точный градиент/тень, идентичные карточке
    «Общий капитал» на «Счетах» (подтверждено визуально бок о бок
    скриншотами обоих экранов в светлой теме).
  - Список категорий: «Продукты» (80%+ — оранжевый прогресс-бар,
    статус «Осталось»), «Подписки»/«Транспорт» (обычный синий
    прогресс), «Красота и уход» (розовый по цвету категории) — все
    карточки белые, без рамки, с одинаковой тенью.
  - Перерасход: искусственно занижен лимит категории «Продукты» до €100
    (потрачено €321) — герой-карточка переключилась на текст
    «Перерасход бюджета» и красное значение `−€53` (та же карточка, тот
    же градиент — не сменила цвет), карточка категории показала красный
    текст «Перерасход €221» и полностью красный прогресс-бар **без**
    рамки вокруг карточки — ровно то, что требовалось.
  - Переключение месяца стрелками `#navrow` (июль → июнь) — суммы
    потрачено/остаток/% пересчитались корректно для другого месяца,
    герой-карточка и карточки категорий обновились.
  - Редактирование лимита (`openBudgetEdit('food')` → поле лимита
    предзаполнено текущим значением → `saveBudget()`) — значение
    сохранено, кнопка «Удалить бюджет» показана только для существующего
    бюджета.
  - Создание нового бюджета (`openBudgetEdit(null)` → выбор категории
    «Кафе и рестораны» → лимит 50 → `saveBudget()`) — новая карточка
    появилась в общем стиле (0% использовано, обычный синий прогресс),
    `state.budgets` обновился корректно; удалена после проверки
    (`deleteBudget()`) — состояние вернулось к исходному демо-набору.
  - Пустое состояние (`state.budgets={}`) — аккуратная белая карточка с
    эмодзи 🎯 и текстом «Бюджетов пока нет. Нажмите «Добавить бюджет».»,
    герой-карточка показывает «Бюджет · Июль» с тем же градиентом и
    текстом «Бюджеты не заданы...».
  - Тёмная тема (`toggleTheme()`) — карточки категорий, тени, герой-
    карточка (градиент/тень, как и ожидается, не адаптируются под тему —
    это заливка, не текстовый цвет) и пустое состояние корректно
    отображены (проверено скриншотом всего экрана).
  - Мобильная ширина 375×812 — без горизонтального переполнения, нижняя
    навигация и скролл не сломаны.
  - `read_console_messages` — без ошибок на протяжении всей проверки
    (единственное сообщение — ожидаемое предупреждение о подавлении
    нативного `confirm()` безголовым браузером, не связано с кодом
    задачи).
- Production-верификация: см. §10 ниже (заполняется после push).

## 10. Production-верификация

_(заполняется после push)_
